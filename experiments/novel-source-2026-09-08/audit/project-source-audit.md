# Project-positive source audit

This bounded audit covers only the three requested normalized GT groups in the frozen Hyperfine v1.20.0 and Tokei v15.0.0 artifacts. It uses the pinned source trees, retained raw symbols, body extents/hashes, and retained binary call evidence. Primary GT/linkage, predictions, scores, builds, and label rules were not changed. Full hashes and evidence are in [project-source-audit.json](project-source-audit.json).

Hyperfine `hyperfine::export::markup::impl_for=T::serialize` is source-consistent. The pinned `src/export/markup.rs` (`sha256=3f38c5fcfc794e3464ae310cbbeff72b98fe2ba437477ede1822c8fd5e07d68e`) has one generic `impl<T: MarkupExporter> Exporter for T` at lines 108-121. Its body selects the unit, computes relative speeds, calls `table_results`, and returns bytes. The three concrete markup types are Asciidoc, Markdown, and Orgmode (`src/export/asciidoc.rs:5-37`, `markdown.rs:6-29`, `orgmode.rs:5-23`); `src/export/mod.rs:103-110` constructs them as `Exporter` trait objects and `write_results` dispatches `Exporter::serialize` at line 136. The three retained members are complete and non-opaque: `FUN_00177190` (`0x77190`, 6650 bytes, body hash `58fccf4e...`), `FUN_0018b930` (`0x8b930`, 6425 bytes, `748d0aaf...`), and `FUN_00192790` (`0x92790`, 5970 bytes, `3558fbe2...`). Their raw symbols all normalize to the same generic impl and their bodies share the unit/relative-speed/table flow. The last body calls the named Orgmode `table_row` implementation at `0x92a40 -> 0x91950`, which is consistent with concrete trait-method instantiation. The different raw hashes are retained as operational identities only; they do not establish separate semantic MonoItems. Verdict: valid one-definition generic witness, high confidence.

Tokei `tokei::cli_utils::Printer<W>::print_results` is also source-consistent. Pinned `src/cli_utils.rs` (`sha256=0f04be73a054e62d1fc78e6e7cbad6e4cffdaae26ce1619d48176ff8f4ca146f`) has one `impl<W: Write> Printer<W>` method, generic over iterator `I`, at lines 317-397. Pinned `src/main.rs` (`sha256=7a6ed68eadf7fa572e2f0d4497f241100684f1b981f4e097b344125e1753fafc`) makes three calls: reverse-sorted `languages.into_iter().rev()` at line 117, sorted `languages.into_iter()` at line 119, and unsorted `languages.iter()` at line 122. Retained `tokei::main` (`0x1d2740`) calls the three members directly: `0x1d3e45 -> 0x1e8150`, `0x1d3f79 -> 0x1e8ff0`, and `0x1d4008 -> 0x1e71a0`. All bodies decode completely with zero opaque indirect jumps: sizes/hashes are 4008/`675e4b43...`, 3741/`4b4a13d7...`, and 3850/`8799c3ec...`, respectively. The three emitted identities are therefore consistent with the one generic method instantiated at three iterator callsites. Raw hashes remain operational evidence, without a semantic MonoItem claim. Verdict: valid one-definition generic witness, high confidence.

Tokei `tokei::language::syntax::SyntaxCounter::parse_context::{{closure}}` has a source-site conflict, with one unresolved exact-site assignment. Pinned `src/language/syntax.rs` (`sha256=3bda9251c5b5ad7f36835e5fb5ca97340064963243763fe60f4db51917279e71`) contains three closure expressions inside `parse_context` (lines 361-553): HTML script MIME parsing at lines 476-479, HTML style parsing with lowercase plus `FromStr` at lines 500-506, and the same lowercase/`FromStr` operation for HTML templates at lines 527-533.

`FUN_0031a7e0` (`0x21a7e0`, 270 bytes, body hash `19805804...`) has raw identity `_ZN5tokei8language6syntax13SyntaxCounter13parse_context28_$u7b$$u7b$closure$u7d$$u7d$17hfeb3fe6d1499779fE` and its body resolves `from_utf8_lossy -> LanguageType::from_mime`; it is the script closure. `FUN_0031a660` (`0x21a660`, 372 bytes, body hash `d1e11b13...`) has raw identity ending `h58da26e3110bd630E` and resolves `from_utf8_lossy -> to_lowercase -> LanguageType::from_str`; it serves the style/template operation. The retained parent body (`0x21acf0`, `perform_multi_line_analysis`, 8609 bytes, hash `5cb78e89...`) has direct calls `0x21bd1a -> 0x21a7e0` and `0x21bc16`, `0x21be22 -> 0x21a660`. Relocations identify `from_utf8_lossy` at `0x427f80`, `to_lowercase` at `0x4270e0`, `FromStr::from_str` at `0x20f020`, and `LanguageType::from_mime` at `0x20a790`.

Thus the normalized positive pair is wrong at the cross-expression source-site level: one member is the script `from_mime` closure and the other is the lowercase `from_str` closure. However, the one `FUN_0031a660` body is reused at both style and template callsites, so the evidence cannot assign that address exclusively to line 500 or line 527. The proposed source-aware sensitivity is a singleton script label for `FUN_0031a7e0` and an explicitly shared/ambiguous style-template label for `FUN_0031a660`. No corrected GT/linkage file was written.

The Hyperfine source is commit `975fe108c4ee7bd2600d10758207b44ca3dae738`; Tokei is commit `c14f744716272fadeb27a74443cdffa0af35f82f`. Raw disambiguator hashes, addresses, and direct calls are operational linkage evidence. They do not prove semantic MonoItems or DefIds. Other generated serde visitor and stat/serialize groups remain unaudited as requested.
