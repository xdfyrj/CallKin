# Hexyl `run::{{closure}}` source audit

The current positive pair `hexyl::run::{{closure}}` is a normalized-label false positive at source-site granularity. The two members are in the non-generic `fn run() -> Result<()>` (`src/main.rs:269`), but they implement different closure expressions:

- `FUN_0015b260` (`0x5b260`, 208 bytes) is the `reader.seek(...).map_err(|_| anyhow!(...))` closure at `src/main.rs:342-348`. Its body constructs `anyhow::Error::msg` from the retained string at rodata `0x117ac`, "Failed to jump to the desired input position...", and `hexyl::run` calls it at `0x58df9` on the seek-error path.
- `FUN_0015b1a0` (`0x5b1a0`, 187 bytes) is the `parse_byte_count` closure at `src/main.rs:353-357`, capturing `block_size` and calling `hexyl::parse_byte_offset` (`0x56bb0`). `hexyl::run` calls it at `0x58e23` for `parse_byte_count(&opt.display_offset)` (`src/main.rs:393`).

The raw symbols share only the normalized `hexyl::run::{{closure}}` path and have different disambiguator hashes. The decisive evidence is semantic: distinct direct callsites in the same `run` body, different function extents/bytes, the source-specific seek-error string in one body, and the parser call in the other. The closure-site ordinal is absent from the normalized label, so the existing GT/linkage group of two is conflated.

The authorized versioned secondary sensitivity split is recorded in [hexyl.gt.v2.json](hexyl.gt.v2.json), [hexyl.linkage.v2.json](hexyl.linkage.v2.json), and [hexyl-source-site-sensitivity.json](hexyl-source-site-sensitivity.json): `hexyl::run::{{closure}}@src/main.rs:342` -> `FUN_0015b260` and `hexyl::run::{{closure}}@src/main.rs:353` -> `FUN_0015b1a0`, preserving both raw mangled identities. The frozen primary GT/linkage files were not overwritten. Because this pair is absent from every retained accepted cluster (V0, exact-token, B2, C3, and rescue), applying the split would change the label universe by one positive-to-negative unpredicted pair (`FN -1`, `TN +1`) without changing `TP` or `FP` for these predictions.

The two previously unobserved members labeled `anyhow::context::impl_for=core::result::Result<T,E>::context` remain plausible instances of one dependency source definition: `anyhow-1.0.91/src/context.rs:42-56` implements the generic `Context<T,E>::context` method. The retained callers are the `--length` context at `src/main.rs:360` (`0x58e94` -> `FUN_001737d0`) and the `--display-offset` context at `src/main.rs:393` (`0x5a208` -> `FUN_00173730`). Their distinct bodies and hashes are compatible with generic/callsite specialization; this bounded audit found no second source definition requiring a split.

All claims are conditional on the frozen normalized labels and retained observation artifacts. See [hexyl-source-audit.json](hexyl-source-audit.json) for full addresses, raw identities, body hashes, source hashes, callsite evidence, and the proposed correction record. No new F4, rebuild, target execution, primary-label change, or method tuning was performed.
