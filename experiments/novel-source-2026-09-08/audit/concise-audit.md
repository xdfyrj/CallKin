# fd closure site audit

The three retained members of the normalized origin `regex_automata::meta::regex::Builder::build_many_from_hir::{{closure}}` all carry a symbol consistent with the `move || strat.create_cache()` closure at `regex-automata/src/meta/regex.rs:3613`. They are 38-byte, byte-identical bodies emitted in three compilation units (`globset`, `regex`, and `regex_automata`), which explains the three disambiguator hashes as emitted copies. Those hashes and compilation units establish retained symbol/codegen identities, not independently distinct source definitions. The preceding `map(|hir| hir.borrow())` closure at line 3607 is not the target: it has no retained symbol and has no captured `Arc<dyn Strategy>`/trait-vtable call matching the target body.

`FUN_00347e80` is reached through the cache-pool vtable constructed in `regex_automata::meta::regex::Builder::build_many` at `0x247908`; `FUN_003804f0` is reached through the corresponding vtable at `0x27ff78`; and `FUN_003b0160` is reached from `Builder::build` at `0x2b0c08`. The first two vtable drop-glue symbols explicitly contain `build_many_from_hir<Hir>`. The third body is also installed in a distinct clone vtable at `0x439f68`, constructed by `Regex::clone` at `0x2b522f`, for the separate source closure at `regex.rs:1916`. Thus `FUN_003b0160` maps to two distinct source closure definitions at one code address: its retained Builder symbol supports membership in the existing normalized origin, while its source-origin set is ambiguous.

The normalized function-level GT/linkage label remains supported as a retained symbol membership, but it is not an exclusive source-identity label for `FUN_003b0160`. Under `label_pair`, the authorized secondary overlay adds `<regex_automata::meta::regex::Regex as core::clone::Clone>::clone::{{closure}}` to that address's source-origin set, making every pair touching it `ambiguous-neutral`. Frozen GT/linkage/predictions/metrics remain unchanged. The audit is offline and did not execute the target.

For this three-member group, that sensitivity view leaves `FUN_00347e80`-`FUN_003804f0` positive and makes the two pairs involving `FUN_003b0160` ambiguous-neutral. The first two hashes/compilation units remain evidence of emitted copies; they are not treated as proof of distinct source definitions or semantic monomorphizations.

See [evidence.json](evidence.json) for hashes, complete target bytes, symbols, vtable slots, call references, callers, and limitations. See [sensitivity-audit.md](sensitivity-audit.md) and [sensitivity.json](sensitivity.json) for the authorized post-hoc scoring sensitivity.
