
# Preserve byte-identical retained research records on every platform.
docs/results/*.json -text
experiments/concrete-null/** -text
docs/protocols/wl-depth.md -text
*.bin binary

# Preserve upstream license notices without applying project whitespace rules.
third_party/licenses/** -whitespace
